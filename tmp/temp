Here is what is happening:

CRHS system which keeps metadata of policy documents (including filenet url of the document) publishes changes to Solace.
We need a listner service that read these metadatchanges from Solace, gets the metadata file information, connects to CRHS bucket in minio to read the metadata file and stores the metdata in Potsgres.
It then reads the filenet URL from metdata, connects to filenet, downloads and stores the document in minio CIQ bucket as raw files.
The next step is to parse the raw files, chunk it and enrich metadata and store in feature store.
This is then published to Elaticsearch (that was missing in the diagram) and vector index is created in elastic search.
I want to do this entire orchestration using wither temporal or Airflow.
Steps 2 and 3 will be in Java and 4 to 6 will be in python.
Before you ceate a revised diagram, can you suggest what orchestrator should I use?
